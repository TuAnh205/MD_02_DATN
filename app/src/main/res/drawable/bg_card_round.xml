<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Màu nền -->
    <solid android:color="@android:color/white"/>

    <!-- Bo góc -->
    <corners android:radius="18dp"/>

    <!-- Viền -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"/> <!-- xám nhạt giống UI -->

</shape>